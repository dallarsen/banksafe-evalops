"""MLflow and OpenTelemetry tracking integration.

Stage 5 will populate this package with:

- mlflow_run.py      — experiment context manager wrapping eval runs
- otel_setup.py      — OpenTelemetry tracer provider + exporters
- artifacts.py       — judge-output and trajectory artifact logging

Until then, this package is intentionally empty.
"""
